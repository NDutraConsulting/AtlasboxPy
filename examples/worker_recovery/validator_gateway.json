{
  "upstream_error": [
    {"action": "retry", "retry": {"max_attempts": 3, "backoff_base_seconds": 0.01}},
    {"action": "redirect", "redirect": {"target": "degraded_create_user"}},
    {"action": "queue", "queue": {"queue_name": "recovery.upstream_error"}}
  ],
  "rate_limited": [
    {"action": "retry", "retry": {"max_attempts": 5, "backoff_base_seconds": 0.01, "backoff_multiplier": 2.0}}
  ],
  "permission_denied": [
    {"action": "fail"}
  ]
}
